import { SecodsToTimePipe } from './secods-to-time.pipe';

describe('SecodsToTimePipe', () => {
  it('create an instance', () => {
    const pipe = new SecodsToTimePipe();
    expect(pipe).toBeTruthy();
  });
});
